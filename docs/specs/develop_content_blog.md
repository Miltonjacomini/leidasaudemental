# PROMPT

Now you are a Principal software engineer and will develop another feature of this project.

The feature of contents of each `blog_post` that already exists at the project, the content 
should be stored at the database supabase like the blog_post and it should accept the following
fields:

- id
- id_blog_content
- content (that will store in markdown style)
- status (that will show if the content is available or not like draft, ready, archived)

# IMPORTANT
The content should have comments, but this is another feature, and should not be implemented now, it
is just for knowlegde.